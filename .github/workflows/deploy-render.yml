name: Deploy to Render

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  NODE_VERSION: '18'

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run linting
      run: npm run lint:check
      
    - name: Build application
      run: npm run build
      
    - name: Test build output
      run: |
        # Vérifier que le build a créé les fichiers nécessaires
        if [ ! -d "dist" ]; then
          echo "❌ Build failed: dist directory not found"
          exit 1
        fi
        
        # Vérifier la taille du bundle (optimisation éco-conception)
        BUNDLE_SIZE=$(du -sh dist | cut -f1)
        echo "📦 Bundle size: $BUNDLE_SIZE"
        
        # Vérifier que les assets sont présents
        if [ ! -f "dist/index.html" ]; then
          echo "❌ Build failed: index.html not found"
          exit 1
        fi
        
        echo "✅ Build successful"
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-files
        path: |
          dist/
          backend/
          package.json
          package-lock.json
        retention-days: 7

  deploy-to-render:
    needs: test-and-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-files
        path: ./
        
    - name: Deploy to Render
      run: |
        echo "🚀 Deploying to Render..."
        
        # Déclencher un nouveau déploiement via l'API Render
        if [ -n "${{ secrets.RENDER_SERVICE_ID }}" ] && [ -n "${{ secrets.RENDER_API_KEY }}" ]; then
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys" \
            -d '{"clearCache": "do_not_clear"}' || echo "⚠️ Deploy trigger failed, but continuing..."
        else
          echo "ℹ️ Render secrets not configured, skipping deployment"
        fi
        
    - name: Verify deployment
      run: |
        # Attendre que le déploiement soit terminé
        sleep 30
        
        # Vérifier que l'application répond
        if [ -n "${{ secrets.RENDER_URL }}" ]; then
          echo "🔍 Checking deployment at ${{ secrets.RENDER_URL }}"
          
          # Test de connectivité
          if curl -f -s "${{ secrets.RENDER_URL }}/api/server" > /dev/null; then
            echo "✅ API endpoint responding"
          else
            echo "⚠️ API endpoint not responding"
          fi
          
          # Test de la page principale
          if curl -f -s "${{ secrets.RENDER_URL }}" > /dev/null; then
            echo "✅ Main page responding"
          else
            echo "⚠️ Main page not responding"
          fi
        else
          echo "ℹ️ RENDER_URL not configured, skipping verification"
        fi

  eco-performance-check:
    needs: deploy-to-render
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install ecoindex-cli
      run: npm install -g ecoindex-cli
      
    - name: Run eco performance check
      run: |
        echo "🌱 Running eco performance check on deployed application..."
        
        # Attendre que l'application soit complètement déployée
        sleep 60
        
        # Exécuter ecoindex sur l'application déployée
        ecoindex-cli "${{ secrets.RENDER_URL }}" --output json > eco-results-production.json
        
        # Extraire et afficher les résultats
        GRADE=$(cat eco-results-production.json | jq -r '.grade')
        SCORE=$(cat eco-results-production.json | jq -r '.score')
        DOM_ELEMENTS=$(cat eco-results-production.json | jq -r '.dom_elements')
        REQUESTS=$(cat eco-results-production.json | jq -r '.requests')
        SIZE=$(cat eco-results-production.json | jq -r '.size')
        
        echo "📊 Eco Performance Results:"
        echo "  Grade: $GRADE"
        echo "  Score: $SCORE/100"
        echo "  DOM Elements: $DOM_ELEMENTS"
        echo "  Requests: $REQUESTS"
        echo "  Size: $SIZE KB"
        
        # Vérifier que l'application respecte les critères éco-conception
        if [ "$SCORE" -ge 85 ]; then
          echo "✅ Excellent score éco-conception: $SCORE/100"
        elif [ "$SCORE" -ge 70 ]; then
          echo "✅ Bon score éco-conception: $SCORE/100"
        elif [ "$SCORE" -ge 50 ]; then
          echo "⚠️ Score éco-conception acceptable: $SCORE/100"
        else
          echo "❌ Score éco-conception à améliorer: $SCORE/100"
          exit 1
        fi
        
    - name: Upload eco results
      uses: actions/upload-artifact@v4
      with:
        name: eco-results-production
        path: eco-results-production.json
        retention-days: 30
      if: always()

  notify-deployment:
    needs: [deploy-to-render, eco-performance-check]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Deployment Summary
      run: |
        echo "🚀 Deployment Summary"
        echo "===================="
        echo "Branch: ${{ github.ref_name }}"
        echo "Commit: ${{ github.sha }}"
        echo "Author: ${{ github.actor }}"
        echo "Deployment URL: ${{ secrets.RENDER_URL }}"
        
        if [ "${{ needs.deploy-to-render.result }}" == "success" ]; then
          echo "✅ Deploy: SUCCESS"
        else
          echo "❌ Deploy: FAILED"
        fi
        
        if [ "${{ needs.eco-performance-check.result }}" == "success" ]; then
          echo "✅ Eco Check: SUCCESS"
        elif [ "${{ needs.eco-performance-check.result }}" == "skipped" ]; then
          echo "⏭️ Eco Check: SKIPPED"
        else
          echo "❌ Eco Check: FAILED"
        fi 